# Stan CLI — environment config
# Copy to .env and fill in values

# Server
PORT=7420
AUTH_TOKEN=changeme-REQUIRED        # openssl rand -hex 32
ROOT_DIR=/home/kay2

# Ollama (local AI)
OLLAMA_URL=http://127.0.0.1:11434

# Remote Browser — Chrome DevTools Protocol (optional)
# Start Chrome: chromium-browser --remote-debugging-port=9222 --no-sandbox &
# CDP_PORT=9222

# External AI keys (for future multi-model routing)
# CLAUDE_API_KEY=
# OPENAI_API_KEY=
# GEMINI_API_KEY=
